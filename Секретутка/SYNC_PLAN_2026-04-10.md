# ПЛАН СИНХРОНИЗАЦИИ ДОКУМЕНТАЦИИ И ИСПРАВЛЕНИЯ КРИТИЧЕСКИХ БАГОВ

**Дата:** 2026-04-10  
**Основание:** 19 исправлений от пользователя + 3 найденных бага  
**Цель:** Подготовить систему к реальному онбордингу с игроком

---

## 📊 СТАТУС НА СЕГОДНЯ

### ✅ Что РАБОТАЕТ (и было ошибочно классифицировано):
1. FSM онбординга существует в коде `1.0 Sekretutka_Game_Rules.json`
2. Quest Architect режимы реализованы
3. ENGINE логирует события и проверяет attempt fail
4. main router маршрутизирует intent
5. PROFILE_GAME — единственная базовая таблица

### 🔴 Что СЛОМАНО (найдено):
1. **THERAPIST_WORKFLOW_ID** — неверный ID в main.json (используется finance вместо therapist)
2. **EVENT_TYPE словарь** — не создан, требуется реестр
3. **STUCK по времени** — не реализовано (25 часов), только денежный STUCK

### 🟠 Что ТРЕБУЕТ ПРОВЕРКИ:
1. XP_TOTAL расчёт в ENGINE
2. Goal decomposition запись в правильные ячейки PROFILE_GAME
3. Checkpoint архитектура онбординга
4. Main.json полная корректность

---

## 🎯 ФАЗА 1: ИСПРАВЛЕНИЕ КРИТИЧЕСКИХ БАГОВ (2-3 часа)

### 1.1 THERAPIST_WORKFLOW_ID (5 минут)
**Файл:** `1. Sekretutka_main.json`  
**Строка:** 672-680

**Действие:**
```bash
# 1. Найти реальный workflowId терапевта в n8n
# 2. Заменить "xFmyWUFSK1CwQxCC54lRF" на реальный ID
# 3. Обновить cachedResultName: "1.5. Sekretutka_Therapist"
# 4. Сохранить и экспортировать
```

**Проверка:**
```
Условие: therapist intent → вызов правильного workflow
Тест: Отправить "Мне грустно" в Telegram → должна включиться терапевт, не финансы
```

---

### 1.2 EVENT_TYPE РЕЕСТР (30 минут)

**Создать новый лист или документ:** `EVENT_TYPES_REGISTRY.md`

**Структура:**
```markdown
# EVENT_TYPE Dictionary

| EVENT_TYPE | CATEGORY | DESCRIPTION | TRIGGER | REQUIRED_FIELDS | OPTIONAL_FIELDS |
|---|---|---|---|---|---|
| ATTEMPT_STARTED | ATTEMPT | Attempt created or resumed | After user first interaction after FAILED | attempt_id | xp_total_at_start |
| ATTEMPT_FAILED | ATTEMPT | Attempt failed after 5 days silence | ENGINE CRON | attempt_id, days_silent | xp_gold_penalty |
| STUCK_TIME_DETECTED | STUCK | Silence threshold detected (25 hours) | ENGINE CRON | attempt_id | silent_hours |
| STUCK_TRIGGERED | BOSS | Stuck condition confirmed | ENGINE CRON | stuck_type, goal_action_count | money_net_window |
| HABIT_DONE | HABIT | Habit logged as done | Quest Architect habit branch | habit_id, status | value, resource_type |
| HABIT_SKIPPED | HABIT | Habit logged as skipped | Quest Architect habit branch | habit_id, status | value, resource_type |
| BELIEF_RECORDED | BELIEF | Therapist recorded a new belief or insight | therapist workflow | belief_text | emotion_context |
| HP_SPENT | THERAPY | HP spent during therapy work | therapist workflow | hp_delta | trigger_reason |
| THERAPY_DISCUSSION | THERAPY | Intermediate therapist discussion | therapist workflow | trigger_reason | method |
| THERAPY_SESSION_COMPLETED | THERAPY | Final therapist session completed | therapist workflow | belief_text | category |
| LEVEL_UP_INTERVIEW_STARTED | LEVEL | Level-up interview started | therapist workflow | level_name | trigger_reason |
| LEVEL_UP_APPROVED | LEVEL | Level-up approved | therapist workflow | level_name | reason |
| LEVEL_UP_HELD | LEVEL | Level-up held for more work | therapist workflow | level_name | reason |
| FIVE_FAILURES_ANALYZED | THERAPY | Five-failure pattern analyzed | therapist workflow | outcome | new_target_json |
| TARGET_REVIEWED | GOAL | Goal decision fixed via STATUS | therapist workflow | status | new_target_json |
| STAGE_CHANGED | STAGE | Stage progressed (EARLY→MID→LATE) | ENGINE CRON | stage_new, stage_previous, stage_xp | derived_stage |
| THERAPIST_REQUIRED | THERAPY | Therapist session required (5+ fails) | ENGINE CRON | fail_count | reframe_type |
| THERAPY_REFRAME_STARTED | THERAPY | Base rule reframe started | therapist workflow | trigger_reason | rule_change_request |
| THERAPY_REFRAME_COMPLETED | THERAPY | Base rule reframe completed | therapist workflow | approved_rule_ids | summary |
| RULE_CREATED | RULE | Base rule or game rule created | game_rules workflow | rule_id | rule_group |
| RULE_UPDATED | RULE | Rule changed in GAME_RULES | game_rules or therapist workflow | rule_id | rule_change |
| COMMIT_LOG | COMMIT | Rules commit accepted | game_rules workflow | commit_type | notes |
| ONBOARDING_COMPLETED | ONBOARDING | Onboarding fully completed | game_rules workflow | rule_count | commit_type |
| ONBOARDING_CHECKPOINT | ONBOARDING | Onboarding progress checkpoint saved by session_id | game_rules workflow | session_id, current_state | draft_json |
| CONSISTENCY_CHECKED | COMMIT | Daily consistency evaluated | ENGINE MIDNIGHT run | consistency_status | energy_signals_count |
| LEVEL_READY_REACHED | LEVEL | Level threshold reached | ENGINE CRON | level_name | xp_total |
```

**Действие:**
1. Держать этот список синхронизированным с `EVENT_TYPE_Registry.json`
2. Не вводить новые EVENT_TYPE без обновления registry и active workflows
3. Обновить ENGINE для валидации при каждом logRow()

---

### 1.3 STUCK ПО ВРЕМЕНИ (1 час)

**Файл:** `1.1 Sekretutka_ENGINE_updated.json`

**Текущее состояние (строка ~597):**
```javascript
// STUCK только по деньгам
stuckDetected = goalActionsCount > 0 && moneyNetWindow < stuckX;
```

**Требуемое изменение:**
```javascript
// Вариант 1: STUCK по времени (25 часов тишины)
let stuckByTime = false;
let hoursOfSilence = silentDays * 24; // упрощение
if (hoursOfSilence >= 25 && attemptActive && goalActionsCount > 0) {
  stuckByTime = true;
  // логировать STUCK_TRIGGERED с reason="SILENT_25_HOURS"
}

// Вариант 2: STUCK по деньгам (текущее)
stuckByMoney = goalActionsCount > 0 && moneyNetWindow < stuckX;

// Объединить
stuckDetected = stuckByTime || stuckByMoney;
```

**Действие:**
1. Добавить параметр `STUCK_HOURS_THRESHOLD` в GAME_RULES (default 25)
2. Обновить ENGINE логику
3. Логировать оба типа STUCK в GAME_LOG с разными reason полями
4. Обновить PRD раздел STUCK

**Проверка:**
```
Тест: Имитировать 25+ часов тишины → должно быть STUCK_TRIGGERED логировано
Тест: Имитировать 10 дней активности но < 5000 THB → должно быть STUCK_TRIGGERED логировано
```

---

## 📋 ФАЗА 2: ДОКУМЕНТАЦИЯ СИНХРОНИЗАЦИЯ (2-3 часа)

### 2.1 Обновить README.md
- [ ] ENGINE_updated.json вместо ENGINE_v4.json
- [ ] Уточнить статус THERAPIST_WORKFLOW_ID (исправлено)
- [ ] Добавить EVENT_TYPE реестр
- [ ] Добавить STUCK типы

### 2.2 Обновить PRD
- [ ] Раздел 4.11 (STUCK) — добавить 25-часовую версию
- [ ] Раздел 5.3 (EVENT_TYPE) — привести EVENT_TYPE список
- [ ] Раздел 4.3 (Attempt) — уточнить FAILED vs STUCK разницу

### 2.3 Обновить AUDIT документ
- [ ] Заменить AUDIT_2026-04-10.md на AUDIT_2026-04-10_REVISED.md
- [ ] Добавить CRITICAL_ISSUES_FOUND.md в master checklist

### 2.4 Создать SETUP.md для новых разработчиков
```markdown
# Как запустить Секретутку с нуля

1. Создать копию Google Sheets template
2. Установить Google Sheets credential в n8n
3. Установить Telegram credential в n8n
4. Импортировать в n8n:
   - 1. Sekretutka_main.json (router)
   - 1.0 Sekretutka_Game_Rules.json (FSM + onboarding)
   - 1.1 Sekretutka_ENGINE_updated.json
   - 1.3 Quest_Architect_Daily_Planner.json
   - 1.5 Sekretutka_Therapist.json
   - 1.4 Sekretutka_finance.json
5. Настроить webhook для Telegram bot
6. Протестировать /start command
```

---

## 🔍 ФАЗА 3: ПРОВЕРКА КОРРЕКТНОСТИ (3-4 часа)

### 3.1 XP_TOTAL Расчёт
**Проверить в ENGINE (строка ~337+):**

Требуемая формула:
```
XP_TOTAL = cumulative(0.5 × XP_Gold gains + 0.5 × HP-positive gains)
```

Текущая реализация:
```javascript
const xpTotal = toNumber(snapshot.XP_TOTAL) ?? 0;
```

Вопрос: Считается ли XP_TOTAL правильно при HP восстановлении?

**Действие:**
1. Проверить, есть ли логика добавления к XP_TOTAL в ENGINE при HP-восстановлении
2. Если нет, добавить
3. Задокументировать расчёт в PRD с примером

---

### 3.2 Goal Decomposition Запись
**Проверить в Quest Architect (1.3 файл):**

Требуемая структура:
- Columns: G, L, Q, V, AA, AF, AK, AP, AU, AZ (10 step slots)
- Rows: 17-35 (19 rows, но используется 10)
- Deadline: Row 37
- Reward: Row 38

**Действие:**
1. Проверить, пишет ли GOAL_DECOMPOSER режим именно туда
2. Убедиться валидация на макс 10 шагов
3. Протестировать decompose goal → проверить PROFILE_GAME

---

### 3.3 Checkpoint Архитектура
**Проверить в Game Rules (1.0 файл):**

Требуемая схема:
```json
{
  "session_id": "145771888",  // Telegram chat_id
  "current_state": "HABIT_1",  // FSM step
  "draft_data": {...},
  "timestamp": "2026-04-10T14:30:00Z"
}
```

**Вопросы:**
- Где это хранится? В текущей модели checkpoint хранится в `GAME_LOG` как `ONBOARDING_CHECKPOINT`, а финал онбординга отдельно логируется как `ONBOARDING_COMPLETED`.
- Как session_id передается между сообщениями?
- Как система вспоминает current_state при следующем сообщении?

**Действие:**
1. Документировать checkpoint архитектуру
2. Убедиться, что session_id передается в каждом сообщении
3. Протестировать: начать онбординг → пауза → возобновить → должна помнить шаг

---

## 📊 МАТРИЦА ЗАВЕРШЕНИЯ

```
ФАЗА 1: КРИТИЧЕСКИЕ БАГИ (Целевая дата: сегодня 18:00)
├─ 1.1 THERAPIST_WORKFLOW_ID                  [ ] 5 min
├─ 1.2 EVENT_TYPE реестр                      [ ] 30 min
└─ 1.3 STUCK по времени (25 часов)            [ ] 1 hour
   Итого: ~2 часа

ФАЗА 2: ДОКУМЕНТАЦИЯ (Целевая дата: завтра утром)
├─ 2.1 README обновление                      [ ] 30 min
├─ 2.2 PRD обновление                         [ ] 45 min
├─ 2.3 AUDIT обновление                       [ ] 15 min
└─ 2.4 SETUP.md создание                      [ ] 30 min
   Итого: ~2 часа

ФАЗА 3: ПРОВЕРКИ (Целевая дата: завтра вечером)
├─ 3.1 XP_TOTAL расчёт                        [ ] 1 hour
├─ 3.2 Goal decomposition запись              [ ] 45 min
└─ 3.3 Checkpoint архитектура                 [ ] 1 hour
   Итого: ~3 часа

ВСЕГО: ~7 часов работы перед реальным онбордингом
```

---

## ✅ КРИТЕРИЙ ГОТОВНОСТИ К ОНБОРДИНГУ

Система готова к реальному онбордингу, когда:

1. ✅ THERAPIST_WORKFLOW_ID исправлен и протестирован
2. ✅ EVENT_TYPE реестр создан и валидируется в ENGINE
3. ✅ STUCK по времени реализован и протестирован
4. ✅ XP_TOTAL расчёт проверен и правильный
5. ✅ Goal decomposition запись проверена
6. ✅ Checkpoint система работает (продолжение онбординга)
7. ✅ README и PRD синхронизированы с кодом
8. ✅ SETUP.md позволяет новому разработчику запустить с нуля

---

## 📝 СЛЕДУЮЩИЙ НЕМЕДЛЕННЫЙ ШАГ

**Для пользователя:**
1. Скажи реальный workflowId терапевта из n8n
2. Подтверди, что хочешь исправлять в таком порядке (Фаза 1 → 2 → 3)
3. Готов ли я начать с THERAPIST_WORKFLOW_ID?

**Я готов:**
- Создать точные инструкции по каждому исправлению
- Выполнить все обновления в JSON файлах
- Протестировать каждое изменение
- Синхронизировать документацию
